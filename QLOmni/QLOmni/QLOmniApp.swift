import SwiftUI

@main
struct QLOmniApp: App {
    var body: some Scene {
        WindowGroup {
            Text("QLOmni — QuickLook Preview Extension host. This app does nothing on its own.")
                .padding()
                .frame(minWidth: 400, minHeight: 100)
        }
    }
}
